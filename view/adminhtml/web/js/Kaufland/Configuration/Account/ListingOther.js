define(['knockout'], function (ko) {
    return {
        synchronization: ko.observable(),
        mappingMode: ko.observable(),
        mappingSkuMode: ko.observable(),
        mappingEanMode: ko.observable(),
    }
});
